#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {

	   char* keyword[] = {"and", "begin", "def", "end",
		                       "else", "eq", "gt", "if", "or",
		                        "print", "return", "while", "identifier"};

printf("%d",	   strlen(keyword));
}
